<div class="modal-header">
    <button type="button" class="close" ng-click="cancel()" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
    <h4 class="modal-title">Register New User</h4>
</div>
<div class="modal-body">
    <form role="form" name="editForm" novalidate>
        <div class="form-group">
            <label for="editEmail" class="control-label">Email:</label>
            <input name="editEmail" type="email" class="form-control" required ng-model="email" />
            <div ng-show="editForm.$submitted || editForm.editName.$dirty">
                <span class="text-danger" ng-show="editForm.editEmail.$error.required">You need to give a valid email address.</span>
            </div>
        </div>
        <div class="form-group">
            <label for="editRole" class="control-label">Role:</label>
            <select id="editRole" class="form-control" ng-model="role">
                @{
                    var dbContext = new BikeTracker.Models.ApplicationDbContext();
                    foreach (var r in dbContext.Roles)
                    {
                        <option value="@r.Name">@r.DisplayName</option>
                    }
                }
            </select>
        </div>
    </form>
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-primary" ng-disabled="editForm.$invalid" ng-click="ok()">Save</button>
    <button type="button" class="btn btn-default" ng-click="cancel()">Cancel</button>
</div>
